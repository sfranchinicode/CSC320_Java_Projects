# Module 5 Notes

## Objectives
- Store 12 months and 12 temperatures
- Let the user: 
  - Enter a month --> get that months temperature
  - Enter "year" --> get 
     - All months and temps
     - Yearly average
     - Highest temp
     - Lowest temp

## Notes
- What two types of inputs does the program need to handle?
   - Two string types:
      - Month names 
      - "yearly"
- What should happen differently depending on input? 
   -
- How will you store each array? 
   - each array will be stored by itself: 
      - months[]
      - temps[]
- How will you make sure the relationships are correct? 
   - I will need to match indexes, IE months[0] = January and temps[0] = 32 degrees
- If a user types march vs March how will it be handles? 
   - String.toLowerCase() or String.toUpperCase() or String.equalsIgnoreCase()

